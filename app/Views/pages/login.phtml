<?php // app/Views/pages/login.phtml ?>

<div class="container-fluid p-0 login-screen">
    <!-- row h-100 garante que a linha ocupe toda altura definida no CSS -->
    <div class="row g-0 h-100">
        
        <!-- LADO ESQUERDO -->
        <div class="col-lg-6 login-sidebar">
            <div class="login-overlay"></div>
            <div class="content text-center text-white p-5">
                <img src="<?= URL_BASE ?>/assets/img/logo_limpa.png" alt="Logo" class="login-logo-big">
                <h1 class="display-5 fw-bold mb-3">Controle Token</h1>
                <p class="lead text-white-50">Gerencie seus Tokens com agilidade.</p>
            </div>
            <div class="shape-1"></div>
            <div class="shape-2"></div>
        </div>

        <!-- LADO DIREITO -->
        <div class="col-lg-6 bg-white-custom">
            <div class="login-form-wrapper">
                
                <!-- Logo Mobile -->
                <div class="text-center d-lg-none mb-4">
                    <img src="<?= URL_BASE ?>/assets/img/logo_limpa.png" alt="Logo" width="60">
                </div>

                <div class="mb-4">
                    <h3 class="fw-bold text-dark">Bem-vindo(a)</h3>
                    <p class="text-muted">Insira seus dados para acessar.</p>
                </div>

                <?php if (isset($error) && $error): ?>
                    <div class="alert alert-danger shadow-sm border-0 mb-4" role="alert">
                        <small><i class="fas fa-exclamation-circle me-1"></i> <?= $error ?></small>
                    </div>
                <?php endif; ?>

                <form action="<?= URL_BASE ?>/login" method="POST">
                    
                    <div class="form-floating mb-3">
                        <input type="email" class="form-control" id="email" name="email" placeholder="E-mail" required>
                        <label for="email">E-mail</label>
                    </div>

                    <div class="form-floating mb-3">
                        <input type="password" class="form-control" id="senha" name="senha" placeholder="Senha" required>
                        <label for="senha">Senha</label>
                    </div>

                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" id="remember">
                            <label class="form-check-label text-muted small" for="remember">Lembrar</label>
                        </div>
                        <a href="<?= URL_BASE ?>/recuperar-senha" class="text-decoration-none small fw-bold">Esqueceu?</a>
                    </div>

                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary btn-login">
                            Entrar
                        </button>
                    </div>

                </form>
                
                <div class="text-center mt-4">
                    <small class="text-muted">&copy; 2026 Clínica Assista</small>
                </div>
            </div>
        </div>
    </div>
</div>