<?php // app/Views/partials/navbar.phtml 
use App\Core\Auth;

// Lógica para pegar apenas o primeiro nome
$nomeCompleto = Auth::name() ?? 'Usuário';
$partesNome = explode(' ', trim($nomeCompleto));
$primeiroNome = $partesNome[0];

?>
<!-- Mudamos para navbar-light bg-white para combinar com o novo CSS -->
<nav class="navbar navbar-expand-lg navbar-light bg-white px-4 shadow-sm border-bottom">
    <a class="navbar-brand d-flex align-items-center" href="<?= URL_BASE ?>/home">
        <img src="<?= URL_BASE ?>/assets/img/logo_limpa.png" width="30" height="30" class="d-inline-block align-top me-2" alt="">
        <span>Controle<span class="text-secondary fw-light">Token</span></span>
    </a>
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav align-items-center">
            <!-- Avatar / Nome -->
            <li class="nav-item dropdown me-3 d-flex align-items-center">

            
                <div class="d-flex flex-column text-end me-2 d-none d-lg-block">
                    <span class="text-gray-600 small fw-bold"><?= $primeiroNome ?? 'Usuário' ?></span>
                    <span class="text-xs text-muted">Online</span>
                </div>
                <div class="bg-primary text-white rounded-circle d-flex align-items-center justify-content-center" style="width: 35px; height: 35px;">
                    <i class="fas fa-user"></i>
                </div>
            </li>
            
            <li class="nav-item border-start ps-3">
                <a class="btn btn-sm text-danger fw-bold" href="<?= URL_BASE ?>/logout">
                    <i class="fas fa-sign-out-alt me-1"></i> Sair
                </a>
            </li>
        </ul>
    </div>
</nav>